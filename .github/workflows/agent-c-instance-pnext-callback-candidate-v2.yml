name: Agent C Vulkan instance pNext callback candidate v2

on:
  workflow_dispatch:
  push:
    branches: [ci/agent-c-vulkan-load-sigill-20260814]
    paths: [.github/workflows/agent-c-instance-pnext-callback-candidate-v2.yml]

permissions:
  contents: read

env:
  FEX_SHA: c011366706eaf65a00380003989b3a10811212b6
  DEBIAN_FRONTEND: noninteractive
  ROOTFS: ${{ github.workspace }}/rootfs-amd64
  HOST: ${{ github.workspace }}/host
  R: ${{ github.workspace }}/receipts
  FEX_PORTABLE: 1

jobs:
  candidate:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          repository: teamleaderleo/FEX
          ref: ${{ env.FEX_SHA }}
          path: src
          submodules: recursive
          fetch-depth: 1

      - name: Dependencies and identity
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p "$R" "$HOST"
          test "$(git -C src rev-parse HEAD)" = "$FEX_SHA"
          echo "base_fex_sha=$(git -C src rev-parse HEAD)" | tee "$R/identity.txt"
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake ninja-build clang-18 lld-18 libclang-18-dev llvm-18-dev pkg-config \
            gcc-x86-64-linux-gnu g++-x86-64-linux-gnu \
            libcap-dev libgl-dev libx11-dev libx11-xcb-dev libxcb1-dev \
            libxrandr-dev libxrender-dev mesa-vulkan-drivers vulkan-tools \
            vulkan-validationlayers debootstrap
          LVP_ICD="$(find /usr/share/vulkan/icd.d -maxdepth 1 -type f -name 'lvp_icd*.json' | head -1)"
          test -n "$LVP_ICD"
          echo "$LVP_ICD" > "$R/lvp-icd.txt"
          VK_DRIVER_FILES="$LVP_ICD" vulkaninfo --summary >"$R/native-vulkaninfo.out" 2>"$R/native-vulkaninfo.err"

      - name: Build x86 callback-chain probe
        shell: bash
        run: |
          set -euxo pipefail
          cat >/tmp/pnext-callbacks.c <<'EOF'
          #define _GNU_SOURCE
          #include <dlfcn.h>
          #include <stdint.h>
          #include <stdio.h>
          #include <stdlib.h>
          #include <string.h>
          #define VK_NO_PROTOTYPES
          #include <vulkan/vulkan.h>

          static volatile uint32_t utils_count;
          static volatile uint32_t report_count;

          VKAPI_ATTR VkBool32 VKAPI_CALL utils_cb(
              VkDebugUtilsMessageSeverityFlagBitsEXT severity,
              VkDebugUtilsMessageTypeFlagsEXT types,
              const VkDebugUtilsMessengerCallbackDataEXT *data,
              void *user) {
            (void)severity; (void)types; (void)data; (void)user;
            ++utils_count;
            fprintf(stderr, "UNEXPECTED_UTILS_CALLBACK count=%u\n", utils_count);
            fflush(stderr);
            return VK_FALSE;
          }

          VKAPI_ATTR VkBool32 VKAPI_CALL report_cb(
              VkDebugReportFlagsEXT flags, VkDebugReportObjectTypeEXT type,
              uint64_t object, size_t location, int32_t code,
              const char *layer, const char *message, void *user) {
            (void)flags; (void)type; (void)object; (void)location; (void)code;
            (void)layer; (void)message; (void)user;
            ++report_count;
            fprintf(stderr, "UNEXPECTED_REPORT_CALLBACK count=%u\n", report_count);
            fflush(stderr);
            return VK_FALSE;
          }

          static int has_layer(PFN_vkEnumerateInstanceLayerProperties fn, const char *wanted) {
            uint32_t n=0; if (fn(&n,NULL)!=VK_SUCCESS) return 0;
            VkLayerProperties *p=calloc(n?n:1,sizeof(*p)); if(!p) return 0;
            if(fn(&n,p)!=VK_SUCCESS){free(p);return 0;} int ok=0;
            for(uint32_t i=0;i<n;++i) if(!strcmp(p[i].layerName,wanted)) ok=1;
            free(p); return ok;
          }
          static int has_ext(PFN_vkEnumerateInstanceExtensionProperties fn, const char *wanted) {
            uint32_t n=0; if(fn(NULL,&n,NULL)!=VK_SUCCESS) return 0;
            VkExtensionProperties *p=calloc(n?n:1,sizeof(*p)); if(!p) return 0;
            if(fn(NULL,&n,p)!=VK_SUCCESS){free(p);return 0;} int ok=0;
            for(uint32_t i=0;i<n;++i) if(!strcmp(p[i].extensionName,wanted)) ok=1;
            free(p); return ok;
          }

          int main(int argc, char **argv) {
            int mixed = argc > 1 && !strcmp(argv[1], "mixed");
            void *vk=dlopen("libvulkan.so.1",RTLD_NOW|RTLD_LOCAL);
            if(!vk){fprintf(stderr,"DLOPEN=%s\n",dlerror());return 10;}
            PFN_vkCreateInstance create=(PFN_vkCreateInstance)dlsym(vk,"vkCreateInstance");
            PFN_vkDestroyInstance destroy=(PFN_vkDestroyInstance)dlsym(vk,"vkDestroyInstance");
            PFN_vkEnumerateInstanceLayerProperties enum_layers=(PFN_vkEnumerateInstanceLayerProperties)dlsym(vk,"vkEnumerateInstanceLayerProperties");
            PFN_vkEnumerateInstanceExtensionProperties enum_exts=(PFN_vkEnumerateInstanceExtensionProperties)dlsym(vk,"vkEnumerateInstanceExtensionProperties");
            if(!create||!destroy||!enum_layers||!enum_exts) return 11;
            const char *layer="VK_LAYER_KHRONOS_validation";
            int layer_ok=has_layer(enum_layers,layer);
            int utils_ok=has_ext(enum_exts,VK_EXT_DEBUG_UTILS_EXTENSION_NAME);
            int report_ok=has_ext(enum_exts,VK_EXT_DEBUG_REPORT_EXTENSION_NAME);
            fprintf(stderr,"CANDIDATE_PREFLIGHT mode=%s layer=%d utils=%d report=%d\n",mixed?"mixed":"utils",layer_ok,utils_ok,report_ok);
            fflush(stderr);
            if(!layer_ok||!utils_ok||(mixed&&!report_ok)) return 12;

            VkDebugUtilsMessengerCreateInfoEXT utils={
              .sType=VK_STRUCTURE_TYPE_DEBUG_UTILS_MESSENGER_CREATE_INFO_EXT,
              .pNext=NULL,
              .messageSeverity=VK_DEBUG_UTILS_MESSAGE_SEVERITY_VERBOSE_BIT_EXT|VK_DEBUG_UTILS_MESSAGE_SEVERITY_INFO_BIT_EXT|VK_DEBUG_UTILS_MESSAGE_SEVERITY_WARNING_BIT_EXT|VK_DEBUG_UTILS_MESSAGE_SEVERITY_ERROR_BIT_EXT,
              .messageType=VK_DEBUG_UTILS_MESSAGE_TYPE_GENERAL_BIT_EXT|VK_DEBUG_UTILS_MESSAGE_TYPE_VALIDATION_BIT_EXT|VK_DEBUG_UTILS_MESSAGE_TYPE_PERFORMANCE_BIT_EXT,
              .pfnUserCallback=utils_cb,
            };
            VkDebugReportCallbackCreateInfoEXT report={
              .sType=VK_STRUCTURE_TYPE_DEBUG_REPORT_CALLBACK_CREATE_INFO_EXT,
              .pNext=&utils,
              .flags=VK_DEBUG_REPORT_INFORMATION_BIT_EXT|VK_DEBUG_REPORT_WARNING_BIT_EXT|VK_DEBUG_REPORT_PERFORMANCE_WARNING_BIT_EXT|VK_DEBUG_REPORT_ERROR_BIT_EXT|VK_DEBUG_REPORT_DEBUG_BIT_EXT,
              .pfnCallback=report_cb,
            };
            VkApplicationInfo app={.sType=VK_STRUCTURE_TYPE_APPLICATION_INFO,.pApplicationName="fex-pnext-candidate",.apiVersion=VK_API_VERSION_1_0};
            const char *layers[]={layer};
            const char *ext_utils[]={VK_EXT_DEBUG_UTILS_EXTENSION_NAME};
            const char *ext_mixed[]={VK_EXT_DEBUG_UTILS_EXTENSION_NAME,VK_EXT_DEBUG_REPORT_EXTENSION_NAME};
            VkInstanceCreateInfo ici={
              .sType=VK_STRUCTURE_TYPE_INSTANCE_CREATE_INFO,
              .pNext=mixed?(const void*)&report:(const void*)&utils,
              .flags=(VkInstanceCreateFlags)0x80000000u,
              .pApplicationInfo=&app,
              .enabledLayerCount=1,.ppEnabledLayerNames=layers,
              .enabledExtensionCount=mixed?2u:1u,
              .ppEnabledExtensionNames=mixed?ext_mixed:ext_utils,
            };
            fprintf(stderr,"CANDIDATE_BEFORE_CREATE mode=%s utils=%u report=%u\n",mixed?"mixed":"utils",utils_count,report_count); fflush(stderr);
            VkInstance instance=VK_NULL_HANDLE;
            VkResult r=create(&ici,NULL,&instance);
            fprintf(stderr,"CANDIDATE_AFTER_CREATE mode=%s result=%d instance=%p utils=%u report=%u\n",mixed?"mixed":"utils",r,(void*)instance,utils_count,report_count); fflush(stderr);
            if(r!=VK_SUCCESS||instance==VK_NULL_HANDLE) return 20;
            if(utils_count||report_count) return 21;
            destroy(instance,NULL);
            fprintf(stderr,"CANDIDATE_AFTER_DESTROY mode=%s utils=%u report=%u\n",mixed?"mixed":"utils",utils_count,report_count); fflush(stderr);
            if(utils_count||report_count) return 22;
            if(dlclose(vk)!=0) return 23;
            fprintf(stderr,"CANDIDATE_RETURN mode=%s callbacks=0\n",mixed?"mixed":"utils"); fflush(stderr);
            return 0;
          }
          EOF
          x86_64-linux-gnu-gcc -O2 -g -Wall -Wextra -Werror \
            -Isrc/External/Vulkan-Headers/include /tmp/pnext-callbacks.c -ldl -o /tmp/pnext-x86_64
          file /tmp/pnext-x86_64 | tee "$R/probe.file.txt"

      - name: Apply callback-suppression candidate
        shell: bash
        run: |
          set -euxo pipefail
          python3 - <<'PY'
          from pathlib import Path
          p=Path('src/ThunkLibs/libvulkan/Host.cpp')
          s=p.read_text()
          fn=s.index('FEXFN_IMPL(vkCreateInstance)')
          start=s.index('  const VkInstanceCreateInfo* vk_struct_base = a_0;',fn)
          end=s.index('\n\n  VkInstance out;',start)
          old=s[start:end]
          if 'VK_STRUCTURE_TYPE_DEBUG_REPORT_CREATE_INFO_EXT' not in old:
            raise SystemExit('debug-report filter missing from expected source region')
          new='''  const VkInstanceCreateInfo* vk_struct_base = a_0;
          for (const VkBaseInStructure* vk_struct = reinterpret_cast<const VkBaseInStructure*>(vk_struct_base); vk_struct->pNext;) {
            const auto next_type = vk_struct->pNext->sType;
            // Ignore guest callbacks installed for temporary debug callbacks during instance creation.
            if (next_type == VK_STRUCTURE_TYPE_DEBUG_REPORT_CREATE_INFO_EXT ||
                next_type == VK_STRUCTURE_TYPE_DEBUG_UTILS_MESSENGER_CREATE_INFO_EXT) {
              // Overwrite the pNext pointer, ignoring its const-qualifier. Re-check the same
              // predecessor so consecutive callback-bearing nodes are all removed.
              const_cast<VkBaseInStructure*>(vk_struct)->pNext = vk_struct->pNext->pNext;
              continue;
            }
            vk_struct = vk_struct->pNext;
          }'''
          p.write_text(s[:start]+new+s[end:])
          PY
          git -C src diff --check
          git -C src diff -- ThunkLibs/libvulkan/Host.cpp | tee "$R/candidate.diff"
          grep -q 'DEBUG_UTILS_MESSENGER_CREATE_INFO_EXT' "$R/candidate.diff"

      - name: Build focused candidate
        shell: bash
        run: |
          set -euxo pipefail
          CC=clang-18 CXX=clang++-18 cmake -S src -B src/build -G Ninja \
            -DClang_DIR=/usr/lib/llvm-18/lib/cmake/clang -DLLVM_DIR=/usr/lib/llvm-18/lib/cmake/llvm \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo -DUSE_LINKER=lld -DENABLE_LTO=OFF \
            -DENABLE_ASSERTIONS=ON -DBUILD_TESTING=OFF -DBUILD_THUNKS=ON \
            -DBUILD_FEXCONFIG=OFF -DRANGES_NATIVE=OFF -DTUNE_CPU=none -DX86_DEV_ROOTFS=/ \
            >"$R/cmake-host.log" 2>&1
          cmake --build src/build --target FEXServer vulkan-host-64 -- -j"$(nproc)" -v >"$R/build-host.log" 2>&1
          cp src/build/HostLibs_64/libvulkan-host.so "$HOST/libvulkan-host.so"
          cmake -S src/ThunkLibs/GuestLibs -B guest -G Ninja \
            -DBITNESS=64 -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_TOOLCHAIN_FILE="$GITHUB_WORKSPACE/src/Data/CMake/toolchain_x86_64.cmake" \
            -DFEX_PROJECT_SOURCE_DIR="$GITHUB_WORKSPACE/src" \
            -DGENERATOR_EXE="$GITHUB_WORKSPACE/src/build/Bin/thunkgen" -DX86_DEV_ROOTFS=/ \
            >"$R/cmake-guest.log" 2>&1
          cmake --build guest --target vulkan-guest -- -j"$(nproc)" -v >"$R/build-guest.log" 2>&1

      - name: Build amd64 rootfs
        shell: bash
        run: |
          set -euxo pipefail
          sudo debootstrap --arch=amd64 --foreign --variant=minbase --components=main,universe \
            --include=libvulkan1,libx11-6 noble "$ROOTFS" http://archive.ubuntu.com/ubuntu >"$R/debootstrap.log" 2>&1
          sudo find "$ROOTFS" -type f -name '*.deb' -print | sort > "$R/rootfs-debs.txt"
          sed -n 's/^I: Extracting \([^ ]*\)\.\.\.$/\1/p' "$R/debootstrap.log" | sort -u > "$R/rootfs-already-extracted.txt"
          while IFS= read -r deb; do
            pkg="$(dpkg-deb -f "$deb" Package)"
            grep -Fxq "$pkg" "$R/rootfs-already-extracted.txt" && continue
            sudo dpkg-deb -x "$deb" "$ROOTFS"
          done < "$R/rootfs-debs.txt"
          sudo cp guest/libvulkan-guest.so "$ROOTFS/usr/lib/x86_64-linux-gnu/libvulkan.so.1"
          sudo ln -sf libvulkan.so.1 "$ROOTFS/usr/lib/x86_64-linux-gnu/libvulkan.so"
          sudo mkdir -p "$ROOTFS/usr/local/bin"
          sudo install -m755 /tmp/pnext-x86_64 "$ROOTFS/usr/local/bin/pnext-callbacks"
          X11="$ROOTFS/usr/lib/x86_64-linux-gnu/libX11.so.6"
          for sym in XSync XGetVisualInfo XDisplayString; do
            x86_64-linux-gnu-readelf -Ws "$X11" | grep -E " ${sym}(@|$)" | head -1
          done

      - name: Run candidate safety matrix
        shell: bash
        run: |
          set -euxo pipefail
          FEX="$GITHUB_WORKSPACE/src/build/Bin/FEX"
          LVP_ICD="$(cat "$R/lvp-icd.txt")"
          for mode in utils mixed; do
            timeout 90s env VK_DRIVER_FILES="$LVP_ICD" FEX_ROOTFS="$ROOTFS" FEX_THUNKHOSTLIBS="$HOST" \
              "$FEX" /usr/local/bin/pnext-callbacks "$mode" >"$R/fex-$mode.out" 2>"$R/fex-$mode.err"
            echo 0 > "$R/fex-$mode.exit"
            cat "$R/fex-$mode.err"
            grep -q "^CANDIDATE_AFTER_CREATE mode=$mode result=0 .*utils=0 report=0" "$R/fex-$mode.err"
            grep -q "^CANDIDATE_RETURN mode=$mode callbacks=0" "$R/fex-$mode.err"
            ! grep -q '^UNEXPECTED_' "$R/fex-$mode.err"
          done
          printf 'utils=0\nmixed=0\nguest_callbacks=0\ncandidate_status=pass\n' | tee "$R/summary.txt"

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: agent-c-instance-pnext-callback-candidate-v2-${{ github.run_id }}
          path: receipts
          retention-days: 30
